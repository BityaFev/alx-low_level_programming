#include "main.h"
/**
 * print_most_numbers- prints the numbers, from 0 to 9, followed by a new line without 2 and 4
 * Return: no return
 */
void print_most_numbers(void)
{
	int x;

	for (x = 48; x < 58; x++)
	
		if (ch != 50 && ch != 52)
			_putchar(x + '0');
		_putchar('\n');
}
